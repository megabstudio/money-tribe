One-line: The Money Tribe call-to-action button — bright-green primary, with secondary, dark, outline and ghost variants and rounded/pill/sharp corners.

```jsx
<Button variant="primary" size="lg">Join tribe</Button>
<Button variant="secondary">Accept</Button>
<Button variant="outline" shape="pill" leftIcon={<SearchIcon/>}>Search</Button>
<Button variant="dark" disabled>Saved</Button>
```

Variants: `primary` (green fill), `secondary` (light-green), `dark` (forest), `outline`, `outline-dark`, `ghost`.
Sizes: `sm` (32px), `md` (40px), `lg` (48px — Figma default).
Shapes: `rounded` (default), `pill`, `sharp`.
Props: `fullWidth`, `leftIcon`, `rightIcon`, `disabled`. Bold Manrope label, soft press-scale, Radix-style focus ring.
