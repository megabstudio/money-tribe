One-line: Toggle switch with the brand green track for binary on/off settings.

```jsx
<Switch defaultChecked label="Auto-save round-ups" />
<Switch checked={on} onCheckedChange={setOn} />
```
Sizes `sm|md|lg`. Controlled or uncontrolled.
